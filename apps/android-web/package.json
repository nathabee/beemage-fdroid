{
  "name": "beemage-android-web",
  "private": true,
  "version": "0.3.5",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.node.json --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "@types/node": "^25.0.7",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
